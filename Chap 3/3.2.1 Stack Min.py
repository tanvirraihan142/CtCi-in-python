import sys

class stack_with_min(Stack):
    def push(self,push):
        newMin = min(value, minimum())
        Stack.push(NodeWithMin(value,newMin))

    def minimum(self):
        if this.is_empty():
            return sys.maxsize
        else:
            peek().min

class NodeWithMin:
    def __init__(self.v.min1):
        value = v
        this.min = min1

    
